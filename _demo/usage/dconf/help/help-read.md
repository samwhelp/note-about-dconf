

# read

run

``` sh
dconf help read
```

show

```
Usage:
  dconf read [-d] KEY 

Read the value of a key.  -d to read default values.

Arguments:
  KEY         A key path (starting, but not ending with '/')
```
