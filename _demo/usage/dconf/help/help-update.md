

# update

run

``` sh
dconf help update
```

show

```
Usage:
  dconf update [DBDIR] 

Update the system dconf databases

Arguments:
  DBDIR       The databases directory. Default: /etc/dconf/db
```
