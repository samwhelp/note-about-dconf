

# load

run

``` sh
dconf help load
```

show

```
Usage:
  dconf load [-f] DIR 

Populate a subpath from stdin.  -f ignore locked keys.

Arguments:
  DIR         A directory path (starting and ending with '/')
```
